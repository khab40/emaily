Summary
This project is a web application built using React and Node.js, designed to manage surveys. It allows users to create, send, and analyze surveys, integrating with services like SendGrid for email functionality and Stripe for payments. The application features user authentication and utilizes a MongoDB database for data storage.

Technologies
JavaScript, Node.js, React, Express, Mongoose, Passport, SendGrid, Stripe

Structure
The main application code is located in the 'client/src' directory, which contains React components, actions, reducers, and utility functions. The server-side code resides in the root directory, with routes, models, and services organized in their respective folders. Configuration files are found in the 'config' directory, while environment variables are managed through '.env' files. Tests and documentation are likely included but not explicitly detailed in the provided structure.
